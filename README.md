<div dir="rtl">
  
# پروژه ATPlus v5.0 (پایدار، ضد فیلتر، بهینه‌شده)

**توجه:** این پروژه نسخه بهینه‌شده، فوق‌پیشرفته و بدون‌باگ از پروژه [AmirTunnel-Pro](https://github.com/amircpuir/AmirTunnel-Pro) است که با تمرکز بر بالاترین سرعت انتقال دیتا، پایداری مطلق، لغو مصرف منابع سرور و مقاومت در برابر سیستم‌های فیلترینگ (DPI) بازنویسی شده است.

یک تونل معکوس (Reverse TCP Tunnel) فوقِ سریع و هوشمند با قابلیت استفاده آسان و یکپارچه در لینوکس. این ابزار به صورت تخصصی برای دور زدن محدودیت‌های سرعت و ایجاد اتصال بین سرور ایران و سرور خارج طراحی شده است.

---

## ویژگی‌های کلیدی v5.0

### 🛡️ پایداری و خود-ترمیمی
- **Health Check خودکار:** هر ۱۵ ثانیه سلامت تمام نشست‌ها بررسی شده و نشست‌های مرده بلافاصله حذف و بازسازی می‌شوند.
- **Exponential Backoff:** در صورت قطع اتصال، تلاش برای اتصال مجدد هوشمندانه از ۵۰۰ms شروع شده و تا ۳۰ ثانیه فاصله گذاشته می‌شود تا سرور تحت فشار نرود.
- **Session Failover (۳ تلاش):** اگر یک نشست خراب باشد، سیستم ۳ بار نشست‌های دیگر را امتحان کرده و فقط در صورت شکست همه، اتصال کاربر را قطع می‌کند.
- **Session Recycling (عمر ۳۰ دقیقه):** هر نشست حداکثر ۳۰ دقیقه عمر دارد و بعد خودکار بازسازی می‌شود تا ISP نتواند اتصال‌های طولانی‌مدت را محدود (throttle) کند.
- **Graceful Shutdown:** در هنگام خاموشی سرویس، ابتدا اتصالات فعال به طور کامل تمام شده و سپس خاموش می‌شود — بدون از دست رفتن دیتا.

### ⚡ سرعت و عملکرد
- **منوی مدیریت گرافیکی (Interactive CLI):** با تایپ `atplus` یک منوی کامل برای مدیریت تونل باز می‌شود.
- **پایداری صد درصدی دانلودها:** با TCP Half-Close اصولی، هیچ دانلودی وسط کار قطع نمی‌شود.
- **بهینه‌سازی حافظه (Zero-Allocation):** مدیریت نشست‌ها به روش `O(1)` با مصرف RAM حداقلی.
- **لاگ‌گذاری سطح‌بندی شده:** لاگ‌ها با زمان دقیق و سطح `INFO/WARN/ERROR` نوشته می‌شوند و با `journalctl` به راحتی خوانا هستند.

### 🖥️ بهینه‌سازی سطح کرنل لینوکس (خودکار)
هنگام نصب، تنظیمات زیر به صورت خودکار اعمال و ذخیره می‌شوند:
- **BBR Congestion Control** — الگوریتم کنترل ازدحام گوگل برای حداکثر throughput
- **TCP Fast Open** — کاهش لیتنسی هندشیک اتصالات جدید
- **بالا بردن سقف بافرها** — تا ۱۶MB (بدون تغییر مقادیر پیش‌فرض، ضد bufferbloat)
- **Keepalive سریع** — شناسایی اتصالات مرده در ۶۰ ثانیه بجای ۲ ساعت
- **صف اتصالات ۶۵ هزارتایی** — جلوگیری از drop شدن درخواست‌ها تحت بار سنگین
- **غیرفعال‌سازی IPv6** — حذف overhead اضافی
- **TIME_WAIT Reuse** — آزادسازی سریع پورت‌ها
- **Nice=-10** — اولویت CPU بالاتر برای ATPlus نسبت به سایر سرویس‌ها
- **ulimit 1M** — پشتیبانی از تا ۱ میلیون اتصال همزمان

---

## حالت‌های نصب (دوگانه)

هنگام نصب، شما می‌توانید بر اساس نیاز خود یکی از دو حالت زیر را انتخاب کنید:

1. **حداکثر سرعت (Raw TCP Mode):** ترافیک به صورت کاملاً خام، بدون هیچ‌گونه سربار اضافی عبور می‌کند تا پینگ به حداقل و سرعت به حداکثر برسد.
2. **حداکثر امنیت (Anti-DPI Mode):** 
   - **اثر انگشت مرورگر (uTLS):** کل ترافیک تونل را به شکل مرورگر کروم (`HelloChrome_120`) درمی‌آورد.
   - **خرد کردن هوشمند بسته‌ها (TLS Fragmentation):** فقط بسته‌های Client Hello خرد می‌شوند تا سیستم مانیتورینگ کور شود.
   - **تولید گواهی امنیتی پویا:** یک‌بار در استارتاپ، بدون مصرف CPU اضافی.
   - **تزریق نویز (HTTP Decoys):** هدرهای دروغین شبیه‌ساز وبگردی.

---

## راهنمای نصب و راه‌اندازی 

```bash
curl -sL "https://raw.githubusercontent.com/ramin-mahmoodi/ATPlus/main/install.sh?v=$(date +%s)" -o install.sh && bash install.sh
```

### مراحل نصب:
1. نوع سرور را مشخص کنید (ایران یا خارج).
2. حالت تونل را مشخص کنید (**سرعت بالا** یا **امنیت بالا / ضد فیلتر**).
3. پورت پل ارتباطی (مثلاً `443`) و پورت سینک (مثلاً `444`) را وارد کنید.
4. **مهم:** یک رمز عبورِ مشترک و ایمن تعیین کنید. این رمز باید در هر دو سرور کاملاً یکسان باشد.
5. در سرور **خارج**، آی‌پی سرور ایران را وارد کنید.
6. در سرور **ایران**، روش همگام‌سازی پورت‌های Xray (خودکار `y` یا دستی `n`) را انتخاب کنید.

---

## منوی مدیریت ATPlus

پس از نصب، فقط تایپ کنید:

```bash
atplus
```
این دستور یک منوی تعاملی باز می‌کند:
1. مشاهده وضعیت روشن/خاموش بودن تونل
2. مشاهده لاگ‌های زنده سرور
3. ری‌استارت سرویس تونل
4. خاموش کردن تونل
5. روشن کردن تونل
6. آپدیت / نصب مجدد (تغییر پورت، رمز، حالت Anti-DPI و ...)
7. پاکسازی ۱۰۰ درصدی تونل از روی سرور

</div>
